/** North Carolina — fit bounds (SW then NE) and extended max bounds for pan slack. */
export const NC_BOUNDS_LNG_LAT: [[number, number], [number, number]] = [
  [-84.3219, 33.7529],
  [-75.4001, 36.5881],
];

/** Prevents panning far outside the state while allowing a margin. */
export const NC_MAX_BOUNDS_LNG_LAT: [[number, number], [number, number]] = [
  [-87.0, 32.0],
  [-73.0, 38.0],
];

export const GUILD_GOLD = '#C9A84C';
